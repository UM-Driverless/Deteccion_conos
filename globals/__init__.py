# globals.py needs an __init__.py file to behave as a package.
# This allows easy absolute imports

# When running "from globals import *", only these modules will be imported:
# __all__ = ['globals']

print('globals package imported')